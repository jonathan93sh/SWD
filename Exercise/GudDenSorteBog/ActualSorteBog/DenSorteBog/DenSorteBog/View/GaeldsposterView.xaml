<Window
    x:Class="DenSorteBog.GaeldsposterView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
    xmlns:ei="http://schemas.microsoft.com/expression/2010/interactions"
    Title="GaeldsposterView"
    Width="605.667"
    Height="704"
    DataContext="{Binding Source={StaticResource Locator}, Path=GaeldsposterViewModel}">

    <Grid>
        <Label
            Width="76"
            Height="26"
            Margin="262,7,-38,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Content="Gældsposter" />
        <Label
            Width="68"
            Margin="270,636,-38,-362"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Content="{Binding Path=Model.totalGaeld}" />
        <DataGrid Name="skylderList" HorizontalAlignment="Left" Margin="10,33,0,0" VerticalAlignment="Top" Height="598" Width="578"
                  ItemsSource="{Binding Path=Model.PersonsSkyldere, Mode=TwoWay}" IsReadOnly="True"/>

    </Grid>
</Window>
